# ©AngelaMos | 2026
# .goreleaser.yml

version: 2

before:
  hooks:
    - go mod tidy

builds:
  - main: .
    binary: test-audit
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X github.com/CarterPerez-dev/test-audit/internal/app.version={{ .Version }}
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
